/**
 * Worker Placeholder - Phase 1A
 * 
 * Rules:
 * - Không có scheduler.
 * - Không poll database.
 * - Không tự start Run.
 * - Không gọi lifecycle command.
 * - Không tạo market data hoặc ledger.
 * - Không chạy side effect nghiệp vụ.
 */

export function initWorkerPlaceholder() {
  console.log("Worker placeholder initialized (No-Op for Phase 1A)");
}
